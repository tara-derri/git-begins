pour cloner un reppository je fais la commande git clone https://github.com/tara-derri/shell.git
les 3 commndes de push:
git add .
git commit -m
git push
commande pour pull: git pull
Pour ajouter une branche j'utilise la commnde : git branch add-create-branch-command

pour basculer sur cette nouvelle branche je fais: git checkout add-create-branch-command


